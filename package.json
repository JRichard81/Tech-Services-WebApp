{
  "name": "tech-services-api",
  "version": "1.0.0",
  "description": "API JSON Server para Tech-Services",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=18"
  }
}
